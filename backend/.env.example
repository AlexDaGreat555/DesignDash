PORT=5001
NODE_ENV=development

# File uploads (local by default; swap for S3/Cloudinary in production)
UPLOAD_DIR=uploads

# Comma-separated allowed frontend origins for CORS (defaults to http://localhost:3000)
# CORS_ORIGIN=https://your-app.vercel.app,http://localhost:3000
