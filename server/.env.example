PORT=5000
NODE_ENV=development

# File uploads (local by default; swap for S3/Cloudinary in production)
UPLOAD_DIR=uploads

# Optional: Seed the spec library
# DATABASE_URL=postgresql://user:pass@localhost:5432/designdash
